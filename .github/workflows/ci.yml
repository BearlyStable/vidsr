name: tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.11", "3.13"]
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      # ffmpeg because the tool shells out to it and the tests encode their own
      # clips; xvfb so the Tk window can be built and driven without a monitor
      - name: install ffmpeg and a virtual display
        run: sudo apt-get update && sudo apt-get install -y ffmpeg xvfb

      - name: install
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: vidsr runs
        run: vidsr --version

      - name: tests
        run: python tests/run_tests.py

      - name: keep the reconstructions from a failed run
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: vidsr-output-py${{ matrix.python-version }}
          path: .testwork/
          retention-days: 7

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install build twine
      - run: python -m build
      - run: twine check --strict dist/*
